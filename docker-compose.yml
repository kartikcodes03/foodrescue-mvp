services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: foodrescue
      POSTGRES_PASSWORD: foodrescue
      POSTGRES_DB: foodrescue
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
